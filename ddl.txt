CREATE TABLE PESSOA (ID  SERIAL NOT NULL, DTYPE VARCHAR(31), DTNASCIMENTO VARCHAR(255), ENDERECO VARCHAR(255), NOME VARCHAR(255), CPF VARCHAR(255), EMAIL VARCHAR(255), TELEFONE BYTEA, CODIGO INTEGER, SALARIO FLOAT, PRIMARY KEY (ID))
CREATE TABLE VENDA (ID  SERIAL NOT NULL, DTVENDA VARCHAR(255), PAGAMENTO VARCHAR(255), CARRO_ID INTEGER, CLIENTE_ID INTEGER, VENDEDOR_ID INTEGER, PRIMARY KEY (ID))
CREATE TABLE CARRO (ID  SERIAL NOT NULL, CATEGORIA VARCHAR(255), FABRICANTE VARCHAR(255), NOME VARCHAR(255), PRECO FLOAT, PRIMARY KEY (ID))
ALTER TABLE VENDA ADD CONSTRAINT FK_VENDA_VENDEDOR_ID FOREIGN KEY (VENDEDOR_ID) REFERENCES PESSOA (ID)
ALTER TABLE VENDA ADD CONSTRAINT FK_VENDA_CARRO_ID FOREIGN KEY (CARRO_ID) REFERENCES CARRO (ID)
ALTER TABLE VENDA ADD CONSTRAINT FK_VENDA_CLIENTE_ID FOREIGN KEY (CLIENTE_ID) REFERENCES PESSOA (ID)
